# Hedera Network Configuration
# Use 'testnet' for development, 'mainnet' for production
HEDERA_NETWORK=testnet

# Operator Account Credentials
# Your Hedera account ID (format: 0.0.xxxxx)
OPERATOR_ID=0.0.12345

# Your Hedera account private key (ECDSA  private key in hex format)
# NEVER commit this to version control - keep it secret!
OPERATOR_KEY=your_0x_private_key_here

# Optional: Mirror Node Configuration
# Base URL for Hedera Mirror Node REST API
MIRROR_NODE_URL=https://testnet.mirrornode.hedera.com

# Optional: Custom Fee Settings
# Fee amount in HBAR for directory submissions (default: 10)
DIRECTORY_SUBMISSION_FEE=10